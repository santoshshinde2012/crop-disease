"""
Streamlit web application for crop disease detection.

Modules:
    config          -- App-level constants (thresholds, paths, model prefs).
    disease_info    -- Disease database and lookup helpers.
    model_service   -- Checkpoint loading and inference logic.
    ui_components   -- Reusable Streamlit rendering functions.
    streamlit_app   -- Entry point (``streamlit run app/streamlit_app.py``).
"""
